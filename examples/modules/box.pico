class Box[T] {
    value T;

    fn set(value: T) -> void {
        this.value = value;
    }

    fn get() -> T {
        return this.value;
    }
}

fn newBox[T](value: T) -> Box[T] {
    let b: Box[T];
    b.set(value);
    return b;
}
