fn score(n: int) -> int {
    let s: int = 0;

    switch (n) {
        case 0: {
            s = 0;
            break;
        }
        case 1: {
            s = 1;
            break;
        }
        case 2: {
            s = 2;
            break;
        }
        default: {
            s = 3;
            break;
        }
    }

    return s;
}

fn main() -> int {
    let total: int = 0;

    for (let i: int = 0; i < 3; i = i + 1) {
        for (let j: int = 0; j < 3; j = j + 1) {
            let sum: int = i + j;
            let s: int = score(sum);
            total = total + s;
        }
    }

    total = total + 25;

    log.logInfo("Result:");
    printInt(total);

    return 0;
}
